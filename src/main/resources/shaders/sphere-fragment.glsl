#version 410

#include "march.glsl"

out vec4 fragColor;

void main() {
    //sphere sp;
    //sp.position = vec3(0.0);
    //sp.radius = 1.0f;
    //fragColor = distanceSphere(sp.position, sp.radius);
    fragColor = vec4(1.0);
}